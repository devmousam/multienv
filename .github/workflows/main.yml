on:
  pull_request:
    branches:
    - dev
jobs:
  DEV:
    name: deploy to dev
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: run a script
        run: |
          echo "I am running a job in dev environment $API_URL"
      
